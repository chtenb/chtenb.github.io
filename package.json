{
  "name": "chtenb",
  "version": "0.0.1",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "restore": "spago install && npm install",
    "adoc": "node asciidoctor.js",
    "build": "spago build && npm run adoc && echo Ctrl-f5 your browser if non-purescript changes were made!",
    "test": "spago test",
    "adocwatch": "watchexec -w blog/ -e adoc npm run adoc",
    "serve": "parcel serve index.html blog/*.html",
    "release": "rm -rf docs && npm run build && parcel build --dist-dir docs/ './index.html' './blog/*.html' && git checkout -- docs/CNAME",
    "publish": "npm run release && git checkout -- docs/CNAME && git add . && git commit -m publish",
    "clean": "rm -rf .cache .spago .psci_modules node_modules output .parcel-cache"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Chiel92/chiel92.github.io"
  },
  "keywords": [
    "dev"
  ],
  "author": "Chiel ten Brinke",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "asciidoctor": "^2.2.4",
    "asciidoctor-highlight.js": "^0.3.0",
    "asciidoctor-kroki": "^0.13.0",
    "parcel": "next",
    "purescript": "^0.14.1",
    "spago": "^0.20.3",
    "highlight.js": "latest"
  }
}
